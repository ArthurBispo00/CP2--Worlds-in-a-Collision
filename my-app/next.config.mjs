/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['photojournal.jpl.nasa.gov'], // Adiciona o domínio da imagem externa
    },
  };
  
  export default nextConfig;
  